# Install PyTorch with CUDA 12.8 FIRST (required for RTX 5060 / sm_120):
#   pip install torch torchvision torchaudio --index-url https://download.pytorch.org/whl/cu128
# Then install the rest:
#   pip install -r requirements.txt

pandas>=2.0.0
numpy>=1.24.0
scikit-learn>=1.3.0
matplotlib>=3.7.0
seaborn>=0.13.0
transformers>=4.40.0
iterative-stratification>=0.1.7
tqdm>=4.66.0
accelerate>=0.27.0
ipykernel>=6.0.0
